package com.lvchao.test;

import com.lvchao.dfs.namenode.server.DataNodeInfo;

import java.util.ArrayList;
import java.util.Comparator;
import java.util.List;
import java.util.stream.Collectors;

/**
 * @Title: Test04
 * @Package: com.lvchao.test
 * @Description:
 * @auther: chao.lv
 * @date: 2021/12/4 21:32
 * @version: V1.0
 */
public class Test04 {
    public static void main(String[] args) {
       /* List<DataNodeInfo> list = new ArrayList<>();
        DataNodeInfo dataNodeInfo1 = new DataNodeInfo();
        dataNodeInfo1.setStoredDataSize(3L);
        DataNodeInfo dataNodeInfo2 = new DataNodeInfo();
        dataNodeInfo2.setStoredDataSize(1L);
        DataNodeInfo dataNodeInfo3 = new DataNodeInfo();
        dataNodeInfo3.setStoredDataSize(2L);
        list.add(dataNodeInfo1);
        list.add(dataNodeInfo2);
        list.add(dataNodeInfo3);

        list.stream().sorted(Comparator.comparing(DataNodeInfo::getStoredDataSize).reversed()).collect(Collectors.toList());
        list.forEach(System.out::println);*/
        System.out.println( 119 / 20);
    }
}
